# ===========================================
# LB Premium - Docker Environment Variables
# ===========================================
# 
# IMPORTANTE: 
# - Cambia las contraseñas en producción
# - Usa valores seguros para JWT_SECRET
# - Ajusta las URLs según tu dominio

# ===========================================
# Base de Datos PostgreSQL
# ===========================================
POSTGRES_DB=lb_premium
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123
POSTGRES_PORT=5432

# ===========================================
# URLs de Conexión
# ===========================================
# URL completa de la base de datos
DATABASE_URL=postgresql://postgres:postgres123@postgres:5432/lb_premium

# URLs del frontend y admin
FRONTEND_URL=http://localhost:3000
ADMIN_URL=http://localhost:3002

# URL de la API para el frontend
NEXT_PUBLIC_API_URL=http://localhost:3001/api
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001
NEXT_PUBLIC_ENV=development

# ===========================================
# Configuración de la Aplicación
# ===========================================
NODE_ENV=development
PORT=3001
FRONTEND_PORT=3000

# ===========================================
# Seguridad
# ===========================================
# IMPORTANTE: Cambia esto en producción por una clave segura
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ===========================================
# Usuario Administrador (Inicialización)
# ===========================================
# Credenciales del usuario administrador que se crea automáticamente
# IMPORTANTE: Cambia la contraseña en producción
ADMIN_EMAIL=elebe.merch@gmail.com
ADMIN_PASSWORD=u1u2u3u4u5
ADMIN_NAME=Administrador Elebe

# ===========================================
# Configuración para Docker
# ===========================================
# Para Docker, usa estas URLs internas:
# NEXT_PUBLIC_API_URL=http://lb-premium-backend:3001/api
# NEXT_PUBLIC_BACKEND_URL=http://lb-premium-backend:3001
# DATABASE_URL=postgresql://postgres:postgres123@postgres:5432/lb_premium

# ===========================================
# Configuración para Producción
# ===========================================
# Para producción, cambia estas URLs por tu dominio:
# FRONTEND_URL=https://tu-dominio.com
# ADMIN_URL=https://admin.tu-dominio.com
# NEXT_PUBLIC_API_URL=https://api.tu-dominio.com/api
# NEXT_PUBLIC_BACKEND_URL=https://api.tu-dominio.com
# DATABASE_URL=postgresql://usuario:password@servidor:5432/lb_premium

# ===========================================
# Configuración Avanzada
# ===========================================
# Dominios de producción para CORS (separados por comas)
# PRODUCTION_DOMAINS=https://tu-dominio.com,https://admin.tu-dominio.com

# ===========================================
# Instrucciones de Uso
# ===========================================
# 1. Copia este archivo como .env
# 2. Modifica los valores según tu entorno
# 3. En producción, usa contraseñas seguras
# 4. Ajusta las URLs según tu dominio
# 5. Ejecuta: docker-compose up -d